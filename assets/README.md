# assets

Drop your copy of the song here as `khat.mp3`.

The page looks for `assets/khat.mp3` and fades it in when she taps "tap to
begin", then loops it quietly under the letter. If the file is missing the page
falls back to the Spotify player in the song section.

Only add music you own or are allowed to use.
